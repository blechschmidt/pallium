# -*- mode: ruby -*-
# vi: set ft=ruby :

# This defines the vagrant box for building pallium.
Vagrant.configure("2") do |config|
  config.vm.box = "ubuntu/jammy64"
  config.vm.provider "virtualbox" do |vb|
    vb.memory = "4096"
  end
end